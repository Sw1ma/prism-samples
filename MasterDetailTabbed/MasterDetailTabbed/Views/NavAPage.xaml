<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MasterDetailTabbed.Views.NavAPage"
             Title="{Binding Title}">
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Title}" VerticalOptions="Center" HorizontalOptions="CenterAndExpand" />
            <Button Text="Go Back" IsVisible="{Binding IsVisible}" VerticalOptions="Center" Command="{Binding NavigateCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>